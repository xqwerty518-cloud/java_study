package 금2026_09_11_3;

public class 인스턴스_멤버와_정적_멤버의_중복정리 {

	public static void main(String[] args) {
		/**
		 * 지금까지 상속 과정에서 부모의 인스턴스 필드 인스턴스 메서드 정적 필드 정적 메서드와
		 * 동일한 이름으로 자식 클래스에서 멤버를 재 정의했을때 일어나는 상황을 알아봤다. 
		 * 
		 * 여기서 기준점은 값을 읽을 때의 기준을 의미한다. 예를 들어 인스턴스 필드 m이 클래스 A와 
		 * 클래스 B에 모두 있다면 A a = new B() 와 같이 표현했다면 인스턴스 필드의 기준점은 
		 * 선언 타입이므로 a.m은 클래스 A의 필드값을 가르키는 것이다 나머지 멤버도 이와 마찬가지로 
		 * 이해하면 된다. 즉 인스턴스 메서드는 객체가 어떤 생성자로 생성됐는지 나머지는 어떤 타입으로 
		 * 선언됐는지가 기준이 되는 것이다. 
		 */
	}

}
